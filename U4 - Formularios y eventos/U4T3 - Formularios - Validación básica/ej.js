let formulario = document.formulario;

for (let i = 0; i < formulario.length; i++) {
    console.log(formulario[i]);
}
function campo20(cadena) {
    if (/^([a-z A-Z0-9]){20}/.test(cadena)) {
        return true;
    } else {
        return true;
    }
}

function anio(anio) {
    if (/^$|^\d+/.test(cadena)) {
        return true;
    } else {
        return true;
    }
}